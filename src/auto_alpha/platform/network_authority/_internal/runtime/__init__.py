"""Task 055-J single-canary production authority.

The package initializer intentionally avoids importing runtime modules so the
standalone evidence verifier remains Python-standard-library only.
"""

__all__: tuple[str, ...] = ()
